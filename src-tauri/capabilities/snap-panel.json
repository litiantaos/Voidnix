{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "snap-panel",
  "description": "Permissions for the snap panel window",
  "windows": ["snap-panel"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
